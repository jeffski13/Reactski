import React from 'react';

//component for search Bares for YT app
//NOTE: this is a functional compoenent only (as opposed to a class)
const JSearchBare = () => {
  return <input />;
}

//allows other files to use JSearchBare (with import JSearchBare from '.../jBarSearchNewCompExample';)
export default JSearchBare;
